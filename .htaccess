<IfModule mod_rewrite.c>
    RewriteEngine On

    # Forward every request into public/ so only public/index.php handles routing.
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>

DirectoryIndex index.php
